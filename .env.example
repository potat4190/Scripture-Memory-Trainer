# Copy to .env and edit. Every value here has a working default, so .env is
# optional for local development.

# Database. Defaults to a local SQLite file (scripture.db) when unset.
# Phase 5 points this at Supabase Postgres, e.g.
#   postgresql+psycopg://USER:PASSWORD@HOST:5432/postgres
DATABASE_URL=
